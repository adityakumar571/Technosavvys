diff --git a/node_modules/react-native-orientation-locker/src/orientation.android.js b/node_modules/react-native-orientation-locker/src/orientation.android.js
index 18720f4..b7e73da 100644
--- a/node_modules/react-native-orientation-locker/src/orientation.android.js
+++ b/node_modules/react-native-orientation-locker/src/orientation.android.js
@@ -7,8 +7,13 @@
 //
 
 "use strict";
-const OrientationNative = require("react-native").NativeModules.Orientation;
-const { NativeEventEmitter } = require("react-native");
+const { NativeModules, NativeEventEmitter, TurboModuleRegistry } = require("react-native");
+// Support both bridgeless (TurboModuleRegistry) and bridge (NativeModules) modes
+const OrientationNative =
+  (TurboModuleRegistry && TurboModuleRegistry.get && TurboModuleRegistry.get("Orientation")) ||
+  NativeModules.Orientation ||
+  null;
+
 let LocalEventEmitter;
 
 var listeners = {};
@@ -36,12 +41,14 @@ export default class Orientation {
   };
 
   static getOrientation = (cb) => {
+    if (!OrientationNative) { cb("UNKNOWN"); return; }
     OrientationNative.getOrientation((orientation) => {
       cb(orientation);
     });
   };
 
   static getDeviceOrientation = (cb) => {
+    if (!OrientationNative) { cb("UNKNOWN"); return; }
     OrientationNative.getDeviceOrientation((deviceOrientation) => {
       cb(deviceOrientation);
     });
@@ -53,27 +60,27 @@ export default class Orientation {
 
   static lockToPortrait = () => {
     locked = true;
-    OrientationNative.lockToPortrait();
+    if (OrientationNative) OrientationNative.lockToPortrait();
   };
 
   static lockToPortraitUpsideDown = () => {
     locked = true;
-    OrientationNative.lockToPortraitUpsideDown();
+    if (OrientationNative) OrientationNative.lockToPortraitUpsideDown();
   };
 
   static lockToLandscape = () => {
     locked = true;
-    OrientationNative.lockToLandscape();
+    if (OrientationNative) OrientationNative.lockToLandscape();
   };
 
   static lockToLandscapeRight = () => {
     locked = true;
-    OrientationNative.lockToLandscapeRight();
+    if (OrientationNative) OrientationNative.lockToLandscapeRight();
   };
 
   static lockToLandscapeLeft = () => {
     locked = true;
-    OrientationNative.lockToLandscapeLeft();
+    if (OrientationNative) OrientationNative.lockToLandscapeLeft();
   };
 
   // OrientationMaskAllButUpsideDown
@@ -83,10 +90,11 @@ export default class Orientation {
 
   static unlockAllOrientations = () => {
     locked = false;
-    OrientationNative.unlockAllOrientations();
+    if (OrientationNative) OrientationNative.unlockAllOrientations();
   };
 
   static addOrientationListener = (cb) => {
+    if (!OrientationNative) return;
     var key = getKey(cb);
     LocalEventEmitter =
       LocalEventEmitter ?? new NativeEventEmitter(OrientationNative);
@@ -108,6 +116,7 @@ export default class Orientation {
   };
 
   static addDeviceOrientationListener = (cb) => {
+    if (!OrientationNative) return;
     var key = getKey(cb);
     LocalEventEmitter =
       LocalEventEmitter ?? new NativeEventEmitter(OrientationNative);
@@ -129,6 +138,7 @@ export default class Orientation {
   };
 
   static addLockListener = (cb) => {
+    if (!OrientationNative) return;
     var key = getKey(cb);
     LocalEventEmitter =
       LocalEventEmitter ?? new NativeEventEmitter(OrientationNative);
@@ -157,10 +167,11 @@ export default class Orientation {
   };
 
   static getInitialOrientation = () => {
-    return OrientationNative.initialOrientation;
+    return OrientationNative ? OrientationNative.initialOrientation : "PORTRAIT";
   };
 
   static getAutoRotateState = (cb) => {
+    if (!OrientationNative) { cb(true); return; }
     OrientationNative.getAutoRotateState((state) => {
       cb(state);
     });
